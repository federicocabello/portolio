[project]
name = "federico-portfolio-assistant"
version = "0.1.0"
description = "Python API for the AI assistant embedded in Federico Cabello's portfolio"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
  "workers-py",
  "workers-runtime-sdk"
]
